export interface apiFile_Type {
    user: string;
    name: string;
    show: boolean;
    zIndex: number
}

export interface apiBox_Type {
    user: string;
    name: string;
    show: boolean;
    zIndex: number
}
